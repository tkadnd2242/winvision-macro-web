Store future deep learning model files here.

Examples:

- YOLO `.pt` model files
- ONNX exported detectors
- label maps and inference metadata

For the current web dashboard, the default YOLO sample path is `models/best.pt`.
